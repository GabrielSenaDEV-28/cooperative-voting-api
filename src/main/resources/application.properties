spring.application.name=cooperative-voting-api

spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/cooperative_voting}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

spring.jpa.hibernate.ddl-auto=validate

app.callback-base-url=${CALLBACK_BASE_URL:http://localhost:8080}